// Módulo neutral: solo Java puro, sin APIs de Paper ni Velocity.
// Contiene el protocolo de plugin-messaging compartido entre el proxy (Velocity)
// y los servers backend (Paper).

plugins {
    java
}

description = "CoreBau — common (protocolo compartido)"
